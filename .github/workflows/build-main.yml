name: Build & Test Main

on:
  push:
    branches: [ master ]

permissions:
  contents: write

concurrency:
  group: build-main-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: JVM Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: graalvm
          java-version: 25
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: false
      - name: Run JVM Tests
        run: ./gradlew test

  javadoc:
    name: Javadoc Compilation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: graalvm
          java-version: 25
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: false
      - name: Build Javadoc
        run: ./gradlew javadoc

  native-test:
    name: Native Test
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: graalvm
          java-version: 25
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: false
      - id: native-run
        name: Run Tests in Native Image
        continue-on-error: true
        run: ./gradlew nativeTest
      - name: Run Tracing Agent (Fallback)
        if: steps.native-run.outcome == 'failure'
        run: ./gradlew -Pagent=standard test
      - name: Run Tests in Native Image (Fallback)
        if: steps.native-run.outcome == 'failure'
        run: ./gradlew metadataCopy nativeTest

  binary-compatibility:
    name: Binary Compatibility
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: graalvm
          java-version: 25
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: false
      - name: Check Binary Compatibility
        run: ./gradlew checkBinaryCompatibility

  publish-snapshot:
    name: Publish Snapshot
    needs: [build, javadoc, native-test, binary-compatibility]
    if: github.repository == 'Minestom/Minestom'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: graalvm
          java-version: 25
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: false

      - name: Run Tracing Agent
        run: ./gradlew -Pagent=standard test

      - name: Copy Reachability Metadata
        run: ./gradlew -Pagent=standard metadataCopy

      - name: Commit and Push Updated Metadata
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main/resources/META-INF/native-image
          if ! git diff --cached --quiet; then
            git commit -m "chore: update GraalVM reachability metadata [skip ci]"
            git push
          else
            echo "Metadata is already up-to-date."
          fi

      - name: Publish Snapshot
        env:
          MINESTOM_VERSION: master-SNAPSHOT
          SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
          SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSWORD }}
        run: |
          ./gradlew nmcpPublishAggregationToCentralPortalSnapshots
          echo "Version: ${MINESTOM_VERSION}" >> $GITHUB_STEP_SUMMARY
